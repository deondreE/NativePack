const std = @import("std");
const mem = @import("std").mem;

// This parser will read all forms of files, then take it into a format
// that the compiler will understand at a high level.
const html_parser = struct {};

const javascript_parser = struct {};
