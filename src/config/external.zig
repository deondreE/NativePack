/// Prettier config changes, or create your own file and reference it here.
pub const Prettier = struct { defaultFile: []const u8 };

/// Tsconfig changes, or create your own file and reference it here.
pub const TsConfig = struct { defaultFile: []const u8 };

/// Swift lint config changes, or create your own file and reference it here.
pub const SwiftLint = struct { defaultFile: []const u8 };
